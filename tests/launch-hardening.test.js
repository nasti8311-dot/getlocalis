import { calculateSettlementReleaseAt } from "../stripe-webhook.js";
import { getCancellationState, parseBookingDateTime } from "../worker-entry.js";
import test from "node:test";
import assert from "node:assert/strict";
import fs from "node:fs";

const read = (path) => fs.readFileSync(path, "utf8");

test("Stripe webhook claims events atomically", () => {
  const source = read("stripe-webhook.js");
  assert.match(source, /INSERTtest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
s+ORtest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
s+IGNOREtest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
s+INTOtest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
s+stripe_webhook_events/);
  assert.match(source, /duplicate:test("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
s*true/);
  assert.match(source, /DELETEtest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
s+FROMtest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
s+stripe_webhook_eventstest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
s+WHEREtest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
s+event_idtest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
s*=/);
});

test("refund reconciliation is wired to settlement ledger", () => {
  const source = read("stripe-webhook.js");
  assert.match(source, /syncChargeRefundstest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
(env,test("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
s*(?:latest)?ChargeIdtest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
)/);
  assert.match(source, /stripe_refund_events/);
  assert.match(source, /applyRefundToSettlementtest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
(env,test("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
s*paymentIntentIdtest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
)/);
  assert.match(source, /reverseProviderTransfertest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
(env,test("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
s*settlementtest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
.provider_transfer_id/);
  assert.match(source, /settlement_statustest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
s*=test("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
s*['"]refunded['"]/);
});

test("settlement creation keeps existing rows pending and release-dated", () => {
  const source = read("stripe-webhook.js");
  assert.match(source, /settlement_statustest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
s*=test("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
s*['"]pending['"]/);
  assert.match(source, /release_at/);
  assert.doesNotMatch(
    source,
    /UPDATEtest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
s+booking_settlementstest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
s+SET[^;]*settlement_statustest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
s*=test("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
s*['"]ready['"]/is
  );
});

test("booking cancellation is race-protected and idempotent", () => {
  const source = read("worker-entry.js");
  assert.match(source, /statustest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
s*=test("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
s*['"]cancellation_processing['"]/);
  assert.match(source, /statustest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
s*=test("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
s*['"]confirmed['"]test("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
s+ANDtest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
s+payment_statustest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
s*=test("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
s*['"]paid['"]/);
  assert.match(source, /fiiviu-cancel-/);
  assert.match(source, /statustest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
s*=test("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
s*['"]cancelled['"],test("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
s*payment_statustest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
s*=test("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
s*['"]refunded['"]/);
  assert.match(source, /statustest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
s*=test("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
s*['"]confirmed['"]/);
});

test("runtime schema evolution is not used by launch paths", () => {
  for (const path of [
    "stripe-webhook.js",
    "worker-entry.js",
    "worker.js",
    "marketplace-entry.js",
    "secure-entry.js"
  ]) {
    const source = read(path);
    assert.doesNotMatch(
      source,
      /test("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
bALTERtest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
s+TABLEtest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
b/i,
      path + " still contains runtime ALTER TABLE"
    );
  }
});

test("production D1 audit workflow checks migration history", () => {
  const source = read(".github/workflows/audit-d1-schema.yml");
  assert.match(source, /SELECT test("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
* FROM d1_migrations ORDER BY id/);
  assert.match(source, /Inspect production D1 schema/);
});

test("launch paths do not mutate D1 schema at request time", () => {
  for (const path of [
    "stripe-webhook.js",
    "worker-entry.js",
    "worker.js",
    "marketplace-entry.js",
    "secure-entry.js",
    "provider-auth.js"
  ]) {
    const source = read(path);
    assert.doesNotMatch(source, /test("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
bCREATEtest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
s+(?:TABLE|INDEX)test("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
s+IFtest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
s+NOTtest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
s+EXISTStest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
b/i, path + " still contains runtime CREATE IF NOT EXISTS");
    assert.doesNotMatch(source, /test("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
bALTERtest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
s+TABLEtest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
b/i, path + " still contains runtime ALTER TABLE");
  }
  const migrations = read("migrations/009_launch_runtime_schemas.sql") + read("migrations/010_legacy_partner_schema.sql");
  assert.match(migrations, /CREATE TABLE IF NOT EXISTS bookings/);
  assert.match(migrations, /CREATE TABLE IF NOT EXISTS experiences/);
  assert.match(migrations, /CREATE TABLE IF NOT EXISTS partner_sessions/);
  assert.match(read("migrations/003_settlement_ledger.sql"), /CREATE TABLE IF NOT EXISTS stripe_webhook_events/);
  const settlementMigration = read("migrations/011_settlement_runtime_fields.sql");
  for (const column of ["provider_ref","provider_name","release_at","settlement_error","settlement_test_transfer_id","settlement_last_attempt_at"]) {
    assert.match(settlementMigration, new RegExp("ADD COLUMN " + column));
  }
});

test("server-side files are excluded from Cloudflare Static Assets", () => {
  const ignore = read(".assetsignore");
  for (const path of [
    "secure-entry.js",
    "marketplace-entry.js",
    "worker-entry.js",
    "worker.js",
    "stripe-webhook.js",
    "provider-auth.js",
  ]) {
    assert.match(
      ignore,
      new RegExp("^" + path.replace(".", "test("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
test("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
.") + "$", "m")
    );
  }
});


test("public static pages never embed a Stripe secret key", () => {
  const source = read("index.html");
  const provider = read("provider.html");
  const admin = read("organizer-admin.html");
  for (const html of [source, provider, admin]) {
    assert.doesNotMatch(html, /sk_(?:test|live)_[A-Za-z0-9]+/);
    assert.doesNotMatch(html, /STRIPE_SECRET_KEY/);
  }
});

test("settlement release helper resolves to experience start", () => {
  assert.equal(
    calculateSettlementReleaseAt("2030-06-15", "14:30"),
    "2030-06-15 11:30:00.000"
  );
});

test("cancellation policy enforces the 24-hour boundary", () => {
  const future = parseBookingDateTime("2030-06-15", "14:30");
  const past = parseBookingDateTime("2020-06-15", "14:30");
  assert.ok(future instanceof Date);
  assert.ok(past instanceof Date);
  assert.equal(getCancellationState({ booking_date: "2030-06-15", booking_time: "14:30" }).allowed, true);
  assert.equal(getCancellationState({ booking_date: "2020-06-15", booking_time: "14:30" }).allowed, false);
  assert.equal(getCancellationState({ booking_date: "not-a-date", booking_time: "14:30" }).allowed, false);
});


test("legacy manual payout path is disabled", () => {
  const source = read("worker-entry.js");
  assert.match(source, /Manual .*Auszahlungen sind deaktiviert/);
  assert.match(source, /}, 410test("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
);/);
});

test("production settlement scheduler is configured", () => {
  const config = read("wrangler.jsonc");
  assert.match(config, /"crons"test("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
s*:test("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
s*test("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
[test("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
s*"test("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
*test("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
/15 test("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
* test("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
* test("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
* test("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
*"/);
  const source = read("secure-entry.js");
  assert.match(source, /async scheduledtest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
(controller, env, ctxtest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
)/);
  assert.match(source, /releaseDueProviderSettlementstest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
(envtest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
)/);
});

test("Stripe mode mismatch is guarded", () => {
  const source = read("worker-entry.js");
  assert.match(source, /configuredTestMode/);
  assert.match(source, /eventIsTestMode/);
  assert.match(source, /mode mismatch/);
});

test("Stripe webhook rejects mode mismatch before ledger writes", () => {
  const source = read("stripe-webhook.js");
  assert.match(source, /const stripeSecretKey = Stringtest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
(envtest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
.STRIPE_SECRET_KEY test("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
|test("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
| ""test("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
)test("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
.trimtest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
(test("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
)/);
  assert.match(source, /eventtest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
?test("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
.livemode === false/);
  assert.match(source, /Stripe webhook mode mismatch/);
  const mismatch = source.indexOf("Stripe webhook mode mismatch; event rejected");
  const ledger = source.indexOf("INSERT OR IGNORE INTO stripe_webhook_events");
  assert.ok(mismatch >= 0 && ledger > mismatch);
});


test("marketplace entrypoint stays wired to the current worker implementation", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /import baseWorker from ["']test("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
.test("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
/worker-entrytest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
.js["']/);
  assert.doesNotMatch(source, /worker-entry-v2test("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
.js/);
});

test("marketplace checkout recalculates price and owns booking identity", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const totalAmount=unitPricetest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
*guests/);
  assert.match(source, /bodytest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
.amount=totalAmount/);
  assert.match(source, /bodytest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
.bookingIdtest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
s*=test("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
s*"FV-"/);
  assert.match(source, /cryptotest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
.randomUUIDtest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
(test("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
)/);
  assert.match(source, /provider_connect_account_id/);
});


test("paid booking finalization is idempotent and email-send guarded", () => {
  const source = read("worker-entry.js");
  const start = source.indexOf("async function finalizePaidBooking");
  const end = source.indexOf("async function sendProviderBookingNotification", start);
  const block = source.slice(start, end);
  assert.match(block, /ON CONFLICTtest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
(payment_intent_idtest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
) DO UPDATE/);
  assert.match(block, /confirmation_email_sent_at/);
  assert.match(block, /if test("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
(!booking test("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
|test("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
| bookingtest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
.confirmation_email_sent_attest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
)/);
  assert.match(block, /confirmation_email_sent_at IS NULL/);
  assert.match(block, /sendProviderBookingNotificationtest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
(env, bookingtest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
)/);
  assert.match(block, /provider notification failure must never block the booking/i);
});

test("direct booking finalization requires the PaymentIntent client secret", () => {
  const source = read("worker-entry.js");
  assert.match(source, /clientSecret = Stringtest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
(bodytest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
?test("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
.clientSecret test("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
|test("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
| bodytest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
?test("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
.client_secret/);
  assert.match(source, /paymentIntenttest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
?test("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
.client_secret/);
  assert.match(source, /Payment confirmation credentials do not match/);
  const checkout = read("index.html");
  assert.match(checkout, /finalizedClientSecret/);
});


test("marketplace checkout rejects invalid or past booking times", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /Ein gültiges Buchungsdatum und eine gültige Uhrzeit sind erforderlich/);
  assert.match(source, /bookingStarttest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
.getTimetest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
(test("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
)<=Datetest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
.nowtest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
(test("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
)/);
  assert.match(source, /Europetest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
/Bucharest/);
});


test("settlement release uses an atomic releasing claim and restores pending on failure", () => {
  const source = read("stripe-webhook.js");
  assert.match(source, /settlement_status='releasing'/);
  assert.match(source, /WHERE id=test("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
? AND settlement_status='pending' AND provider_transfer_id IS NULL AND settlement_test_transfer_id IS NULL/);
  assert.match(source, /settlement_status='pending',settlement_error=.*WHERE id=test("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
? AND settlement_status='releasing'/);
  assert.match(source, /Idempotency-Key.*provider-transfer-test("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
$test("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
{paymentIntentIdtest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
}/s);
});

test("settlement release checks refunds before provider transfer", () => {
  const source = read("stripe-webhook.js");
  const claim = source.indexOf("settlement_status='releasing'");
  const transfer = source.indexOf("createProviderTransfer", claim);
  const refund = source.indexOf("Refund detected before provider transfer", claim);
  assert.ok(claim >= 0 && refund >= 0 && transfer > refund);
});


test("worker JSON responses do not default to wildcard CORS", () => {
  const source = read("worker-entry.js");
  const jsonStart = source.indexOf("function json(");
  const jsonEnd = source.indexOf("function isHtmlResponse", jsonStart);
  const block = source.slice(jsonStart, jsonEnd);
  assert.doesNotMatch(block, /Access-Control-Allow-Origin": "test("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
*"/);
});

test("admin CORS is restricted to configured first-party origins", () => {
  const source = read("secure-entry.js");
  assert.match(source, /function getAdminCorstest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
(request, envtest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
)/);
  assert.match(source, /PUBLIC_APP_URL/);
  assert.ok(source.includes("https://fiiviu.ro"));
  assert.match(source, /headerstest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
[test("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
"Access-Control-Allow-Origintest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
"test("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
] = origin/);
  assert.doesNotMatch(source, /const CORS = test("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
{[test("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
test("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
stest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
test("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
S]*Access-Control-Allow-Origin.*test("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
*.*test("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
}/);
});

test("provider sessions do not accept bearer tokens", () => {
  const source = read("provider-auth.js");
  const start = source.indexOf("export function providerSessionFromRequest");
  const end = source.indexOf("export async function createProviderSession", start);
  const block = source.slice(start, end);
  assert.doesNotMatch(block, /Authorization/);
  assert.match(block, /fiiviu_provider_session/);
});

test("provider login keeps the session token cookie-only", () => {
  const source = read("worker-entry.js");
  assert.match(source, /Set-Cookie.*providerSessionCookie/s);
  const loginStart = source.indexOf("async function handleProviderLogin");
  const loginEnd = source.indexOf("async function handleProviderSession", loginStart);
  const loginBlock = source.slice(loginStart, loginEnd);
  assert.doesNotMatch(loginBlock, /sessionTokentest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
s*:/);
});

test("admin CORS is restricted in worker-entry too", () => {
  const source = read("worker-entry.js");
  const adminStart = source.indexOf('request.method === "OPTIONS"');
  const adminEnd = source.indexOf('if (request.method === "POST" && url.pathname === "/api/admin/resend-confirmation")', adminStart);
  const block = source.slice(adminStart, adminEnd);
  assert.match(block, /PUBLIC_APP_URL/);
  assert.ok(block.includes("https://fiiviu.ro"));
  assert.match(block, /Access-Control-Allow-Origin.*origin/s);
  assert.doesNotMatch(block, /Access-Control-Allow-Origin.*test("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
*/s);
});


test("legacy worker payment-intent endpoint is disabled", () => {
  const source = read("worker.js");
  const entry = read("worker-entry.js");
  assert.match(source, /Legacy payment endpoint disabled/);
  assert.match(source, /return jsontest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
(test("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
s*test("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
{ error: test("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
"Legacy payment endpoint disabled/);
  const legacyStart = source.indexOf('if (url.pathname === "/api/create-payment-intent")');
  const legacyBlock = source.slice(legacyStart, legacyStart + 300);
  assert.ok(legacyStart >= 0);
  assert.doesNotMatch(legacyBlock, /paramstest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
.settest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
(test("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
"amounttest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
",Stringtest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
(amounttest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
)test("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
)/);
});

test("secure entry routes payment creation through marketplace worker", () => {
  const source = read("secure-entry.js");
  const marketplaceIndex = source.indexOf('const { default: marketplaceWorker }');
  const fallbackIndex = source.indexOf('return applyApiSecurityHeaders(await marketplaceWorker.fetch(request, env, ctx), request, env);');
  assert.ok(marketplaceIndex >= 0 && fallbackIndex > marketplaceIndex);
});
test("provider auth schema is migration-owned, not runtime-created", () => {
  const source = read("provider-auth.js");
  const migration = read("migrations/007_provider_auth.sql");
  assert.doesNotMatch(source, /CREATE TABLE IF NOT EXISTS provider_(?:accounts|sessions)/);
  assert.match(source, /sqlite_master/);
  assert.match(source, /Provider authentication schema is missing/);
  assert.match(migration, /CREATE TABLE IF NOT EXISTS provider_accounts/);
  assert.match(migration, /CREATE TABLE IF NOT EXISTS provider_sessions/);
});
test("provider marketplace schemas are migration-owned", () => {
  const source = read("worker-entry.js");
  const migration = read("migrations/008_provider_marketplace_schema.sql");
  for (const table of ["providers","offers","provider_payouts"]) {
    assert.doesNotMatch(source, new RegExp("CREATE TABLE IF NOT EXISTS " + table));
    assert.match(migration, new RegExp("CREATE TABLE IF NOT EXISTS " + table));
  }
  assert.match(source, /Providers schema is missing/);
  assert.match(source, /Booking settlement schema is incomplete/);
});


test("organizer admin key is memory-only", () => {
  const source = read("organizer-admin.html");
  assert.match(source, /let adminKey = ""/);
  assert.doesNotMatch(source, /sessionStoragetest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
.getItemtest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
("fiiviu_admin_key"test("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
)/);
  assert.doesNotMatch(source, /sessionStoragetest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
.setItemtest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
("fiiviu_admin_key"/);
  assert.doesNotMatch(source, /sessionStoragetest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
.removeItemtest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
("fiiviu_admin_key"/);
});

test("admin offer deletion is safe around existing bookings", () => {
  const source = read("worker.js");
  assert.match(source, /requesttest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
.method==="DELETE"/);
  assert.match(source, /SELECT id,title,provider_ref,active FROM offers/);
  assert.match(source, /FROM bookings WHERE experience_name=test("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
? AND provider_name=test("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
?/);
  assert.match(source, /UPDATE offers SET active=0,updated_at=CURRENT_TIMESTAMP/);
  assert.match(source, /DELETE FROM offers WHERE id=test("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
?/);
  const admin = read("organizer-admin.html");
  assert.match(admin, /deleteOfferById/);
  assert.match(admin, /method:"DELETE"/);
});


test("admin response CORS is first-party restricted, not wildcard", () => {
  const source = read("worker.js");
  assert.match(source, /const isAdminPath = urltest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
.pathnametest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
.startsWithtest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
("test("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
/apitest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
/admintest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
/"test("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
)/);
  assert.match(source, /allowedAdminOrigins/);
  assert.match(source, /if test("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
(isAdminPathtest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
)/);
  assert.doesNotMatch(source, /isAdminPath[test("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
test("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
stest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
test("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
S]{0,800}Access-Control-Allow-Origin.*test("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
test("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
*.*isAdminPath/);
});

test("secure admin CORS permits the organizer DELETE action", () => {
  const source = read("secure-entry.js");
  assert.match(source, /Access-Control-Allow-Methods": "GET, POST, PATCH, DELETE, OPTIONS"/);
});

test("provider API requires an authenticated cookie session", () => {
  const source = read("worker-entry.js");
  assert.match(source, /authenticateProviderSessiontest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
(request,envtest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
)/);
  assert.match(source, /providerRefFromSessiontest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
(request,envtest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
)/);
  assert.match(source, /const providerRef=await providerRefFromSessiontest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
(request,envtest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
)/);
  assert.match(source, /const provider=await envtest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
.DBtest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
.preparetest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
("SELECT provider_ref,name,contact_email,connect_account_id,active FROM providers WHERE provider_ref=test("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
? LIMIT 1"test("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
)/);
  assert.doesNotMatch(source, /PROVIDER_ACCOUNT_MAP_JSON/);
  assert.doesNotMatch(source, /PROVIDER_ADMIN_KEY/);
  assert.doesNotMatch(source, /STRIPE_PROVIDER_CONNECT_ACCOUNT_ID/);
});


test("legacy marketplace provider auth fallbacks are disabled", () => {
  const source = read("marketplace-entry.js");
  assert.doesNotMatch(source, /PROVIDER_ACCOUNT_MAP_JSON/);
  assert.doesNotMatch(source, /PROVIDER_ADMIN_KEY/);
  assert.doesNotMatch(source, /STRIPE_PROVIDER_CONNECT_ACCOUNT_ID/);
  assert.doesNotMatch(source, /Authorization.*Bearer/s);
  assert.match(source, /authenticateProviderSessiontest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
(request,envtest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
)/);
});

test("marketplace catalog and checkout require an active provider", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /FROM providers WHERE provider_ref=test("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
? AND active=1 LIMIT 1/);
  assert.match(source, /INNER JOIN providers p ON ptest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
.provider_ref=otest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
.provider_ref AND ptest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
.active=1 WHERE otest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
.active=1/);
  assert.match(source, /INNER JOIN providers p ON ptest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
.connect_account_id=etest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
.provider_connect_account_id AND ptest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
.active=1 WHERE etest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
.status='published'/);
});


test("sandbox settlement rows with a test transfer are not reprocessed", () => {
  const source = read("stripe-webhook.js");
  assert.match(source, /settlement_status='pending'.*provider_transfer_id IS NULL.*settlement_test_transfer_id IS NULL/);
  assert.match(source, /settlement_test_transfer_id IS NULL ORDER BY id ASC LIMIT 50/);
});


test("Stripe webhook schema checks are read-only and migration-owned", () => {
  const source = read("stripe-webhook.js");
  assert.match(source, /PRAGMA table_infotest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
(/);
  assert.match(source, /Required D1 table is missing/);
  assert.match(source, /D1 table schema is incomplete/);
  assert.doesNotMatch(source, /test("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
bCREATEtest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
s+(?:TABLE|INDEX)test("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
s+/i);
  assert.doesNotMatch(source, /test("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
bALTERtest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
s+TABLEtest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
b/i);
});


test("static assets carry active baseline browser security headers", () => {
  const source = read("_headers");
  assert.match(source, /^Strict-Transport-Security:test("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
s*max-age=31536000; includeSubDomains$/m);
  assert.match(source, /^X-Frame-Options:test("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
s*DENY$/m);
  assert.match(source, /^X-Content-Type-Options:test("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
s*nosniff$/m);
  assert.match(source, /^Referrer-Policy:test("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
s*strict-origin-when-cross-origin$/m);
  assert.match(source, /^Permissions-Policy:test("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
s*camera=test("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
(test("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
), microphone=test("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
(test("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
), geolocation=test("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
(test("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
), payment=test("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
(selftest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
)$/m);
});


test("cancellation CORS is restricted to first-party origins", () => {
  const source = read("worker-entry.js");
  const start = source.indexOf("async function handleCancellation");
  const end = source.indexOf("async function", start + 20);
  const block = source.slice(start, end > start ? end : start + 5000);
  assert.match(block, /PUBLIC_APP_URL/);
  assert.ok(block.includes("https://fiiviu.ro"));
  assert.match(block, /Access-Control-Allow-Origin.*origin/);
  assert.doesNotMatch(block, /Access-Control-Allow-Origin": "test("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
*"/);
  assert.match(block, /"Vary": "Origin"/);
});

test("cancellation API responses are marked non-cacheable", () => {
  const source = read("worker-entry.js");
  const start = source.indexOf("async function handleCancellation");
  const end = source.indexOf("async function", start + 20);
  const block = source.slice(start, end > start ? end : start + 5000);
  assert.match(block, /"Cache-Control": "no-store"/);
});

test("API responses receive baseline transport and browser security headers", () => {
  const source = read("secure-entry.js");
  assert.match(source, /Strict-Transport-Security.*max-age=31536000; includeSubDomains/);
  assert.match(source, /X-Content-Type-Options.*nosniff/);
  assert.match(source, /Referrer-Policy.*strict-origin-when-cross-origin/);
  assert.match(source, /Permissions-Policy.*payment=test("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
(selftest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
)/);
  assert.match(source, /applyApiSecurityHeaderstest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
(await adminWorkertest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
.fetch/);
  assert.match(source, /applyApiSecurityHeaderstest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
(await marketplaceWorkertest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
.fetch/);
  assert.match(source, /"Strict-Transport-Security": "max-age=31536000; includeSubDomains"/);
  assert.match(source, /"X-Content-Type-Options": "nosniff"/);
  assert.match(source, /"Referrer-Policy": "strict-origin-when-cross-origin"/);
  assert.match(source, /"Permissions-Policy": "camera=test("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
(test("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
), microphone=test("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
(test("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
), geolocation=test("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
(test("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
), payment=test("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
(selftest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
)"/);
});

test("admin settlement endpoints inherit a no-store cache policy", () => {
  const source = read("secure-entry.js");
  const corsStart = source.indexOf("function getAdminCors(request, env)");
  const corsEnd = source.indexOf("function applyApiSecurityHeaders", corsStart);
  const block = source.slice(corsStart, corsEnd);
  assert.match(block, /"Cache-Control": "no-store"/);
});

test("JSON API responses are marked non-cacheable", () => {
  for (const path of ["worker-entry.js", "marketplace-entry.js", "worker.js"]) {
    const source = read(path);
    assert.match(source, /Cache-Control[^test("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
n]*no-store/);
  }
});


test("partner sessions are cookie-only", () => {
  const source = read("worker.js");
  const partnerHtml = read("partner.html");
  const authStart = source.indexOf("async function authenticatePartner");
  const authEnd = source.indexOf("function partnerSessionCookie", authStart);
  const authBlock = source.slice(authStart, authEnd);
  assert.doesNotMatch(authBlock, /Authorization/);
  assert.match(authBlock, /fiiviu_partner_session/);
  assert.match(source, /JSONtest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
.stringifytest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
(test("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
{success:true,partnerRef:Stringtest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
(accounttest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
.partner_reftest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
)test("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
}test("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
)/);
  assert.doesNotMatch(source, /JSONtest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
.stringifytest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
(test("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
{success:true,partnerRef:Stringtest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
(accounttest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
.partner_reftest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
),sessionToken:/);
  assert.doesNotMatch(partnerHtml, /localStoragetest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
.setItemtest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
(tokenKey/);
  assert.doesNotMatch(partnerHtml, /Authorization:'Bearer 'test("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
+token/);
});


test("booking access links are generated in the booking finalization path", () => {
  const worker = read("worker-entry.js");
  const secure = read("secure-entry.js");
  assert.match(worker, /booking_access_token/);
  assert.match(worker, /booking_url:/);
  assert.match(worker, /bookingtest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
.htmltest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
?id=/);
  assert.doesNotMatch(secure, /globalThistest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
.fetchtest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
s*=test("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
s*async function/);
  assert.doesNotMatch(secure, /__fiiviuSecureEmailPatch/);
});


test("admin origin normalization uses a valid end-anchored regex", () => {
  const source = read("worker.js");
  assert.ok(source.includes("replace(/test("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
test("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
/$/"));
});

test("secure entry does not persist request-scoped bindings on globalThis", () => {
  const source = read("secure-entry.js");
  assert.doesNotMatch(source, /globalThistest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
test("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
.__fiiviuDB/);
  assert.doesNotMatch(source, /globalThistest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
test("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
.__fiiviuPublicAppUrl/);
});


test("provider API responses do not expose wildcard CORS", () => {
  const source = read("secure-entry.js");
  assert.match(source, /restrictCors/);
  assert.match(source, /headerstest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
.deletetest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
("Access-Control-Allow-Origin"test("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
)/);
  assert.match(source, /applyApiSecurityHeaderstest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
(await adminWorkertest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
.fetchtest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
(request, env, ctxtest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
), request, env, truetest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
)/);
  assert.match(source, /Cache-Control.*no-store/);
});


test("public marketplace and partner responses do not default to wildcard CORS", () => {
  const marketplace = read("marketplace-entry.js");
  const worker = read("worker.js");
  assert.doesNotMatch(marketplace, /Access-Control-Allow-Origin": "test("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
*"/);
  assert.doesNotMatch(worker, /corsHeaderstest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
["Access-Control-Allow-Origin"test("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
]test("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
s*=test("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
s*"test("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
*"/);
});

test("marketplace checkout derives the payable amount from the server catalog", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const unitPrice=Numbertest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
(experiencetest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
.price_centstest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
)/);
  assert.match(source, /const totalAmount=unitPricetest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
*guests/);
  assert.match(source, /bodytest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
.amount=totalAmount/);
  assert.doesNotMatch(source, /bodytest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
.amount=Numbertest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
(bodytest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
.amount/);
});

test("marketplace checkout replaces client booking IDs with a server-generated ID", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /bodytest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
.bookingId = "FV-" test("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
+ cryptotest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
.randomUUIDtest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
(test("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
)/);
  assert.match(source, /bodytest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
.bookingId = "FV-" test("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
+ cryptotest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
.randomUUIDtest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
(test("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
)test("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
.replacetest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
(test("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
/-test("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
/g, ""test("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
)/);
});

test("marketplace checkout requires a published experience and active provider", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /Stringtest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
(experiencetest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
.statustest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
)!=="published"/);
  assert.match(source, /FROM providers WHERE provider_ref=test("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
? AND active=1 LIMIT 1/);
  assert.match(source, /valid provider Connect account/);
});


test("local Wrangler secret files are excluded from Cloudflare Static Assets", () => {
  const ignore = read(".assetsignore");
  assert.match(ignore, /^test("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
test("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
.devtest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
test("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
.vars$/m);
  assert.match(ignore, /^test("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
test("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
.devtest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
test("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
.varstest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
test("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
.test("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
*$/m);
  assert.match(ignore, /^test("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
test("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
*test("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
.env$/m);
  assert.match(ignore, /^test("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
test("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
*test("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
.envtest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
.test("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
*$/m);
});

test("public static pages never expose server secret names", () => {
  const pages = [
    read("index.html"),
    read("provider.html"),
    read("organizer-admin.html"),
    read("partner.html")
  ].join("test("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
n");
  for (const secretName of [
    "STRIPE_SECRET_KEY",
    "STRIPE_WEBHOOK_SECRET",
    "EMAILJS_PRIVATE_KEY",
    "RESEND_API_KEY",
    "ADMIN_PAYOUT_KEY",
    "CLOUDFLARE_API_TOKEN",
    "CLOUDFLARE_ACCOUNT_ID"
  ]) {
    assert.doesNotMatch(
      pages,
      new RegExp(secretName.replaceAll("_", "[_]"))
    );
  }
});


test("marketplace image URLs are restricted to safe HTTP(S) sources", () => {
  const source = read("index.html");
  assert.match(source, /function safeImageUrltest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
(value, fallback = ""test("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
)/);
  assert.match(source, /urltest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
.protocol === "https:"/);
  assert.match(source, /urltest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
.origin === windowtest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
.locationtest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
.origin && urltest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
.protocol === "http:"/);
  assert.match(source, /imageUrl = safeImageUrltest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
(offertest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
.image_urltest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
)/);
  assert.match(source, /safeImageUrltest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
(imagetest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
)/);
});


test("organizer access can be provisioned and emailed from admin", () => {
  const source = read("worker-entry.js");
  const admin = read("organizer-admin.html");
  assert.match(source, /async function handleAdminProviderPassword/);
  assert.match(source, /INSERT INTO provider_accounts/);
  assert.match(source, /DELETE FROM provider_sessions WHERE provider_ref=test("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
?/);
  assert.match(source, /fiiviu_provider_session/);
  assert.ok(source.includes("https://fiiviu.ro/provider.html"));
  assert.match(source, /emailSent:true/);
  assert.match(source, /temporaryPassword:password/);
  assert.match(admin, /createProviderLoginById/);
  assert.match(admin, /Zugang per E-Mail/);
  assert.match(admin, /test("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
test("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
/apitest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
test("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
/admintest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
test("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
/provider-password/);
});


test("provider session cookie is host-only and malformed cookies fail closed", () => {
  const source = read("provider-auth.js");
  assert.match(source, /__Host-fiiviu_provider_session=/);
  assert.match(source, /Path=test("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
test("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
/; Max-Age=/);
  assert.match(source, /HttpOnly; Secure; SameSite=Lax/);
  assert.match(source, /trytest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
test("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
{return decodeURIComponenttest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
test("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
(matchtest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
test("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
[1test("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
test("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
]test("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
test("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
);test("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
test("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
}catchtest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
test("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
{return "";test("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
test("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
}/);
  assert.doesNotMatch(source, /(^|[^-])fiiviu_provider_session=/);
});


test("admin auth tolerates accidental whitespace around the configured secret", () => {
  const source = read("worker-entry.js");
  assert.ok(source.includes('String(request.headers.get("Authorization") || "").trim()'));
  assert.ok(source.includes('String(env.ADMIN_PAYOUT_KEY || "").trim()'));
});


test("settlement status admin auth trims configured and incoming credentials", () => {
  const source = read("secure-entry.js");
  const start = source.indexOf('url.pathname === "/api/admin/settlement-status"');
  const end = source.indexOf('if (url.pathname.startsWith("/api/admin/")', start);
  const block = source.slice(start, end);
  assert.match(block, /Stringtest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
(requesttest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
.headerstest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
.gettest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
("Authorization"test("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
) test("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
|test("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
| ""test("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
)test("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
.trimtest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
(test("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
)/);
  assert.match(block, /Stringtest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
(envtest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
.ADMIN_PAYOUT_KEY test("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
|test("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
| ""test("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
)test("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
.trimtest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
(test("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
)/);
  assert.doesNotMatch(block, /requesttest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
.headerstest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
.gettest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
("Authorization"test("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
) !== "Bearer " test("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
+ Stringtest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
(envtest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
.ADMIN_PAYOUT_KEYtest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
)/);
});

test("all legacy admin auth comparisons trim configured and incoming credentials", () => {
  const worker = read("worker.js");
  const secure = read("secure-entry.js");
  assert.match(worker, /function isAdmintest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
(request,envtest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
)test("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
{return Stringtest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
(envtest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
.ADMIN_PAYOUT_KEYtest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
|test("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
|""test("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
)test("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
.trimtest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
(test("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
)!==""&&Stringtest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
(requesttest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
.headerstest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
.gettest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
("Authorization"test("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
)test("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
|test("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
|""test("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
)test("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
.trimtest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
(test("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
)==="Bearer "test("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
+Stringtest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
(envtest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
.ADMIN_PAYOUT_KEYtest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
|test("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
|""test("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
)test("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
.trimtest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
(test("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
)test("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
}/);
  assert.doesNotMatch(worker, /requesttest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
.headerstest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
.gettest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
("Authorization"test("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
)==="Bearer "test("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
+envtest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
.ADMIN_PAYOUT_KEY/);
  assert.doesNotMatch(secure, /requesttest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
.headerstest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
.gettest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
("Authorization"test("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
) !== "Bearer " test("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
+ Stringtest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
(envtest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
.ADMIN_PAYOUT_KEYtest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
)/);
  assert.match(secure, /Stringtest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
(requesttest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
.headerstest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
.gettest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
("Authorization"test("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
) test("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
|test("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
| ""test("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
)test("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
.trimtest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
(test("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
)/);
  assert.match(secure, /Stringtest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
(envtest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
.ADMIN_PAYOUT_KEY test("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
|test("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
| ""test("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
)test("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
.trimtest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
(test("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
)/);
});

test("marketplace checkout revalidates that the experience provider is active", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /SELECT provider_ref,name,connect_account_id,active FROM providers WHERE connect_account_id=test("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
? AND active=1 LIMIT 1/);
  assert.match(source, /Experience provider is not currently active/);
  assert.match(source, /bodytest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
.providerName=Stringtest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
(bodyProviderNametest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
|test("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
|experiencetest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
.provider_nametest("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
|test("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
|""test("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
)/);
});
test("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
|test("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
|test("marketplace checkout enforces configured experience time slots", () => {
  const source = read("marketplace-entry.js");
  assert.match(source, /const configuredTimes=normalizeAvailableTimes\(experience\.available_times\)/);
  assert.match(source, /Die gewählte Uhrzeit ist für dieses Erlebnis nicht verfügbar/);
  assert.match(source, /available_times: String\(offer\.available_times\|\|"")/);
  assert.match(source, /function normalizeAvailableTimes\(value\)/);
});
