import { handleProviderConnectRoute } from './provider-connect.js';

export default {
  async fetch(request, env) {
    const url = new URL(request.url);
    const corsHeaders = {"Access-Control-Allow-Origin":"*","Access-Control-Allow-Methods":"GET, POST, PATCH, OPTIONS","Access-Control-Allow-Headers":"Content-Type, Authorization"};
    if (request.method === "OPTIONS") return new Response(null,{status:204,headers:corsHeaders});

    const providerRoute = await handleProviderConnectRoute(request, env, url, corsHeaders);
    if (providerRoute) return providerRoute;

    if (url.pathname === "/api/create-payment-intent") {
      if (request.method !== "POST") return json({error:"Method Not Allowed"},405,corsHeaders);
      try {
        const body=await request.json();
        const amount=Number(body.amount);
        const currency=String(body.currency||"eur").toLowerCase();
        const bookingId=String(body.bookingId||"").trim();
        const tourName=String(body.tourName||"").trim();
        const experienceId=String(body.experienceId||"").trim();
        const guests=Number(body.guests||1);
        const bodyPartnerRef=typeof body.partnerRef==="string"?body.partnerRef.trim():"";
        const urlPartnerRef=url.searchParams.get("ref")?.trim()||"";
        let partnerRef=bodyPartnerRef||urlPartnerRef;
        if(!Number.isInteger(amount)||amount<50)return json({error:"Invalid amount"},400,corsHeaders);
        if(!bookingId)return json({error:"Booking ID fehlt"},400,corsHeaders);
        if(!experienceId)return json({error:"Experience ID fehlt"},400,corsHeaders);
        if(currency!=="eur")return json({error:"Marketplace-Zahlungen sind derzeit nur in EUR freigeschaltet."},400,corsHeaders);
        if(!env.STRIPE_SECRET_KEY)return json({error:"Stripe secret not configured"},500,corsHeaders);
        if(!env.DB)return json({error:"D1 database not configured"},500,corsHeaders);
        await ensureMarketplaceTables(env);
        const mapping=await env.DB.prepare(`SELECT ep.experience_id,ep.provider_id,p.display_name,p.stripe_account_id,p.stripe_payouts_enabled,p.status FROM experience_providers ep JOIN providers p ON p.id=ep.provider_id WHERE ep.experience_id=? AND ep.active=1 LIMIT 1`).bind(experienceId).first();
        if(!mapping)return json({error:"Für dieses Erlebnis ist noch kein Anbieter hinterlegt."},409,corsHeaders);
        if(Number(mapping.stripe_payouts_enabled)!==1 || mapping.status!=="active" || !mapping.stripe_account_id)return json({error:"Der Erlebnisanbieter ist für Auszahlungen noch nicht freigeschaltet."},409,corsHeaders);
        if(partnerRef){await ensurePartnersTable(env);const partner=await env.DB.prepare("SELECT active FROM partners WHERE partner_ref = ? LIMIT 1").bind(partnerRef).first();if(partner && Number(partner.active)!==1) partnerRef="";}
        const providerCents=Math.floor(amount*0.85);
        const partnerCents=partnerRef?Math.round(amount*0.03):0;
        const platformCents=amount-providerCents;
        const recordedPlatformCents=partnerRef?Math.round(amount*0.12):Math.round(amount*0.15);
        if(providerCents<=0 || recordedPlatformCents+providerCents!==amount)return json({error:"Ungültige Abrechnungssumme"},500,corsHeaders);
        const params=new URLSearchParams();
        params.set("amount",String(amount));params.set("currency",currency);
        params.set("metadata[booking_id]",bookingId);params.set("metadata[tour_name]",tourName);params.set("metadata[experience_id]",experienceId);params.set("metadata[provider_id]",String(mapping.provider_id));params.set("metadata[guests]",String(guests));params.set("metadata[provider_share_cents]",String(providerCents));params.set("metadata[platform_share_cents]",String(recordedPlatformCents));params.set("metadata[partner_share_cents]",String(partnerCents));
        if(partnerRef)params.set("metadata[partner_ref]",partnerRef);
        params.set("transfer_data[destination]",mapping.stripe_account_id);params.set("transfer_data[amount]",String(providerCents));params.set("automatic_payment_methods[enabled]","true");
        const stripeResponse=await fetch("https://api.stripe.com/v1/payment_intents",{method:"POST",headers:{"Authorization":"Bearer "+env.STRIPE_SECRET_KEY,"Content-Type":"application/x-www-form-urlencoded"},body:params});
        const data=await stripeResponse.json();if(!stripeResponse.ok)return json({error:data?.error?.message||"Stripe error"},stripeResponse.status,corsHeaders);
        await env.DB.prepare(`INSERT INTO booking_settlements (booking_id,experience_id,provider_id,payment_intent_id,gross_cents,provider_cents,platform_cents,partner_cents,currency,partner_ref,status) VALUES (?,?,?,?,?,?,?,?,?,?,?) ON CONFLICT(booking_id) DO UPDATE SET payment_intent_id=excluded.payment_intent_id,provider_id=excluded.provider_id,experience_id=excluded.experience_id,gross_cents=excluded.gross_cents,provider_cents=excluded.provider_cents,platform_cents=excluded.platform_cents,partner_cents=excluded.partner_cents,currency=excluded.currency,partner_ref=excluded.partner_ref,updated_at=CURRENT_TIMESTAMP`).bind(bookingId,experienceId,Number(mapping.provider_id),data.id,amount,providerCents,recordedPlatformCents,partnerCents,currency,partnerRef||null,"created").run();
        return json({clientSecret:data.client_secret,paymentIntentId:data.id,partnerRef:data.metadata?.partner_ref||"",experienceId,providerId:Number(mapping.provider_id),providerShareCents:providerCents,platformShareCents:recordedPlatformCents,partnerShareCents:partnerCents},200,corsHeaders);
      }catch(error){return json({error:error?.message||"Server error"},500,corsHeaders)}
    }

    if (url.pathname === "/api/admin/experience-providers") {
      if(!env.ADMIN_PAYOUT_KEY)return json({error:"Admin key not configured"},500,corsHeaders);if(!isAdmin(request,env))return json({error:"Unauthorized"},401,corsHeaders);if(!env.DB)return json({error:"D1 database not configured"},500,corsHeaders);
      try{await ensureMarketplaceTables(env);if(request.method==="GET"){const result=await env.DB.prepare(`SELECT ep.experience_id,ep.provider_id,ep.active,ep.created_at,ep.updated_at,p.display_name,p.stripe_account_id,p.status,p.stripe_payouts_enabled FROM experience_providers ep JOIN providers p ON p.id=ep.provider_id ORDER BY ep.experience_id`).all();return json({mappings:result.results||[]},200,corsHeaders)}if(request.method!=="POST"&&request.method!=="PATCH")return json({error:"Method Not Allowed"},405,corsHeaders);const body=await request.json();const experienceId=String(body.experienceId||"").trim();const providerId=Number(body.providerId);if(!experienceId)return json({error:"Experience ID fehlt"},400,corsHeaders);if(!Number.isInteger(providerId)||providerId<=0)return json({error:"Ungültige Provider-ID"},400,corsHeaders);const provider=await env.DB.prepare("SELECT id,display_name,status,stripe_account_id,stripe_payouts_enabled FROM providers WHERE id=? LIMIT 1").bind(providerId).first();if(!provider)return json({error:"Provider nicht gefunden"},404,corsHeaders);if(request.method==="PATCH"){const active=body.active===undefined?1:(body.active===true||body.active===1||body.active==="1"?1:0);await env.DB.prepare("UPDATE experience_providers SET provider_id=?,active=?,updated_at=CURRENT_TIMESTAMP WHERE experience_id=?").bind(providerId,active,experienceId).run()}else{await env.DB.prepare(`INSERT INTO experience_providers (experience_id,provider_id,active) VALUES (?,?,1) ON CONFLICT(experience_id) DO UPDATE SET provider_id=excluded.provider_id,active=1,updated_at=CURRENT_TIMESTAMP`).bind(experienceId,providerId).run()}const mapping=await env.DB.prepare(`SELECT ep.experience_id,ep.provider_id,ep.active,p.display_name,p.stripe_account_id,p.status,p.stripe_payouts_enabled FROM experience_providers ep JOIN providers p ON p.id=ep.provider_id WHERE ep.experience_id=? LIMIT 1`).bind(experienceId).first();return json({success:true,mapping},200,corsHeaders)}catch(error){return json({error:error?.message||"Server error"},500,corsHeaders)}
    }

    if (url.pathname === "/api/partner-stats") {if(request.method!=="GET")return json({error:"Method Not Allowed"},405,corsHeaders);try{const partnerRef=url.searchParams.get("ref")?.trim()||"";if(!partnerRef)return json({error:"Partner-Code fehlt"},400,corsHeaders);return json(await getPartnerStats(env,partnerRef),200,corsHeaders)}catch(error){return json({error:error?.message||"Server error"},500,corsHeaders)}}

    if (url.pathname === "/api/admin/partners") {
      if(!env.ADMIN_PAYOUT_KEY)return json({error:"Admin key not configured"},500,corsHeaders);if(!isAdmin(request,env))return json({error:"Unauthorized"},401,corsHeaders);if(!env.DB)return json({error:"D1 database not configured"},500,corsHeaders);
      try{await ensurePartnersTable(env);if(request.method==="GET"){const result=await env.DB.prepare("SELECT id,name,type,partner_ref,contact_name,contact_email,active,created_at FROM partners ORDER BY created_at DESC, id DESC").all();return json({partners:result.results||[]},200,corsHeaders)}if(request.method==="POST"){const body=await request.json();const name=String(body.name||"").trim();const type=String(body.type||"Hotel").trim();const contactName=String(body.contactName||"").trim();const contactEmail=String(body.contactEmail||"").trim();let partnerRef=String(body.partnerRef||"").trim().toUpperCase();if(!name)return json({error:"Partner-Name fehlt"},400,corsHeaders);if(!partnerRef)partnerRef=await generatePartnerRef(env,name);if(!/^[A-Z0-9_-]{3,32}$/.test(partnerRef))return json({error:"Ungültiger Partner-Code"},400,corsHeaders);const existing=await env.DB.prepare("SELECT id FROM partners WHERE partner_ref = ? LIMIT 1").bind(partnerRef).first();if(existing)return json({error:"Dieser Partner-Code existiert bereits."},409,corsHeaders);const result=await env.DB.prepare("INSERT INTO partners (name,type,partner_ref,contact_name,contact_email,active) VALUES (?,?,?,?,?,1)").bind(name,type||"Hotel",partnerRef,contactName||null,contactEmail||null).run();return json({success:true,partner:{id:result.meta?.last_row_id||null,name,type:type||"Hotel",partnerRef,contactName,contactEmail,active:1,link:buildPartnerLink(partnerRef),qrUrl:buildQrUrl(partnerRef)}},201,corsHeaders)}if(request.method==="PATCH"){const body=await request.json();const id=Number(body.id);if(!Number.isInteger(id)||id<=0)return json({error:"Ungültige Partner-ID"},400,corsHeaders);const current=await env.DB.prepare("SELECT * FROM partners WHERE id = ? LIMIT 1").bind(id).first();if(!current)return json({error:"Partner nicht gefunden"},404,corsHeaders);const name=String(body.name ?? current.name).trim();const type=String(body.type ?? current.type).trim();const contactName=String(body.contactName ?? current.contact_name ?? "").trim();const contactEmail=String(body.contactEmail ?? current.contact_email ?? "").trim();if(!name)return json({error:"Partner-Name fehlt"},400,corsHeaders);const active=body.active===undefined?Number(current.active)!==0:(body.active===true||body.active===1||body.active==="1");await env.DB.prepare("UPDATE partners SET name=?,type=?,contact_name=?,contact_email=?,active=? WHERE id=?").bind(name,type||"Hotel",contactName||null,contactEmail||null,active?1:0,id).run();const updated=await env.DB.prepare("SELECT id,name,type,partner_ref,contact_name,contact_email,active,created_at FROM partners WHERE id = ? LIMIT 1").bind(id).first();return json({success:true,partner:updated},200,corsHeaders)}return json({error:"Method Not Allowed"},405,corsHeaders)}catch(error){return json({error:error?.message||"Server error"},500,corsHeaders)}
    }

    if (url.pathname === "/api/admin/partner-payout") {
      if(!env.ADMIN_PAYOUT_KEY)return json({error:"Payout admin key not configured"},500,corsHeaders);if(!isAdmin(request,env))return json({error:"Unauthorized"},401,corsHeaders);if(!env.DB)return json({error:"D1 database not configured"},500,corsHeaders);
      try{await ensurePayoutsTable(env);const partnerRef=url.searchParams.get("ref")?.trim()||"";if(request.method==="GET"){if(!partnerRef)return json({error:"Partner-Code fehlt"},400,corsHeaders);const partner=await env.DB.prepare("SELECT id,name,type,partner_ref,active FROM partners WHERE partner_ref = ? LIMIT 1").bind(partnerRef).first();if(!partner)return json({error:"Partner nicht gefunden"},404,corsHeaders);const result=await env.DB.prepare("SELECT id,partner_ref,amount_cents,payout_date,status,reference,created_at FROM partner_payouts WHERE partner_ref = ? ORDER BY payout_date DESC, id DESC").bind(partnerRef).all();return json({partner,payouts:(result.results||[]).map(p=>({...p,amount:Number(p.amount_cents)/100}))},200,corsHeaders)}if(request.method!=="POST")return json({error:"Method Not Allowed"},405,corsHeaders);const body=await request.json();const bodyPartnerRef=String(body.partnerRef||"").trim();const amountCents=Number(body.amountCents);const payoutDate=String(body.payoutDate||"").trim();const reference=String(body.reference||"").trim();if(!bodyPartnerRef)return json({error:"Partner-Code fehlt"},400,corsHeaders);if(!Number.isInteger(amountCents)||amountCents<=0)return json({error:"Invalid payout amount"},400,corsHeaders);if(!/^\d{4}-\d{2}-\d{2}$/.test(payoutDate))return json({error:"Invalid payout date"},400,corsHeaders);const partner=await env.DB.prepare("SELECT id,name,active FROM partners WHERE partner_ref = ? LIMIT 1").bind(bodyPartnerRef).first();if(!partner)return json({error:"Partner nicht gefunden"},404,corsHeaders);if(Number(partner.active)!==1)return json({error:"Dieser Partner ist deaktiviert."},400,corsHeaders);const stats=await getPartnerStats(env,bodyPartnerRef);const openCommissionCents=Math.round(stats.openCommission*100);if(openCommissionCents<0)return json({error:"Für diesen Partner besteht aktuell ein negativer Provisionssaldo durch Rückerstattungen nach früheren Auszahlungen.",openCommission:stats.openCommission,paidCommission:stats.paidCommission},400,corsHeaders);if(amountCents>openCommissionCents)return json({error:"Auszahlung ist höher als die offene Provision.",openCommission:stats.openCommission,requestedAmount:amountCents/100},400,corsHeaders);if(reference){const existing=await env.DB.prepare("SELECT id FROM partner_payouts WHERE partner_ref = ? AND reference = ? LIMIT 1").bind(bodyPartnerRef,reference).first();if(existing)return json({error:"Diese Auszahlungsreferenz existiert bereits."},409,corsHeaders)}const result=await env.DB.prepare("INSERT INTO partner_payouts (partner_ref,amount_cents,payout_date,status,reference) VALUES (?,?,?,'paid',?)").bind(bodyPartnerRef,amountCents,payoutDate,reference||null).run();const updatedStats=await getPartnerStats(env,bodyPartnerRef);return json({success:true,payoutId:result.meta?.last_row_id||null,partnerRef:bodyPartnerRef,amount:amountCents/100,payoutDate,reference:reference||null,openCommission:updatedStats.openCommission,paidCommission:updatedStats.paidCommission},201,corsHeaders)}catch(error){return json({error:error?.message||"Server error"},500,corsHeaders)}
    }

    const assetResponse = await env.ASSETS.fetch(request);
    if(request.method === "GET" && (url.pathname === "/" || url.pathname === "/index.html")){
      return new HTMLRewriter().on("body",{element(element){element.append(`<script>\n(function(){\n  const MARKETPLACE_API = ${JSON.stringify(url.origin + '/api/create-payment-intent')};\n  window.createPaymentIntent = async function({amountEUR, bookingId, tourName, guests}) {\n    const partnerRef = ((typeof currentPartnerRef !== 'undefined' ? currentPartnerRef : '') || localStorage.getItem('fiiviu_partner_ref') || new URLSearchParams(window.location.search).get('ref') || '').trim();\n    const experienceId = ((typeof currentTourKey !== 'undefined' ? currentTourKey : '') || '').trim();\n    if(!experienceId) throw new Error('Erlebnis-ID fehlt.');\n    const response = await fetch(MARKETPLACE_API + '?ref=' + encodeURIComponent(partnerRef), {method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({amount:Math.round(Number(amountEUR)*100),currency:'eur',bookingId,tourName,guests:Number(guests),partnerRef,experienceId})});\n    const data = await response.json();\n    if(!response.ok) throw new Error(data && data.error ? data.error : 'Zahlung konnte nicht vorbereitet werden.');\n    return data;\n  };\n})();\n</script>`,{html:true})}}).transform(assetResponse);
    }
    return assetResponse;
  }
};

function isAdmin(request,env){return request.headers.get("Authorization")==="Bearer "+env.ADMIN_PAYOUT_KEY}
async function ensurePartnersTable(env){await env.DB.prepare(`CREATE TABLE IF NOT EXISTS partners (id INTEGER PRIMARY KEY AUTOINCREMENT,name TEXT NOT NULL,type TEXT NOT NULL DEFAULT 'Hotel',partner_ref TEXT NOT NULL UNIQUE,contact_name TEXT,contact_email TEXT,active INTEGER NOT NULL DEFAULT 1,created_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP)`).run();try{await env.DB.prepare("ALTER TABLE partners ADD COLUMN active INTEGER NOT NULL DEFAULT 1").run()}catch(e){}}
async function ensurePayoutsTable(env){await env.DB.prepare(`CREATE TABLE IF NOT EXISTS partner_payouts (id INTEGER PRIMARY KEY AUTOINCREMENT,partner_ref TEXT NOT NULL,amount_cents INTEGER NOT NULL CHECK (amount_cents > 0),payout_date TEXT NOT NULL,status TEXT NOT NULL DEFAULT 'paid',reference TEXT,created_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP)`).run();await env.DB.prepare("CREATE INDEX IF NOT EXISTS idx_partner_payouts_partner_ref ON partner_payouts(partner_ref)").run();await env.DB.prepare("CREATE INDEX IF NOT EXISTS idx_partner_payouts_payout_date ON partner_payouts(payout_date)").run()}
async function ensureMarketplaceTables(env){await env.DB.prepare(`CREATE TABLE IF NOT EXISTS experience_providers (experience_id TEXT PRIMARY KEY,provider_id INTEGER NOT NULL,active INTEGER NOT NULL DEFAULT 1,created_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP,updated_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP)`).run();await env.DB.prepare(`CREATE TABLE IF NOT EXISTS booking_settlements (id INTEGER PRIMARY KEY AUTOINCREMENT,booking_id TEXT NOT NULL UNIQUE,experience_id TEXT NOT NULL,provider_id INTEGER NOT NULL,payment_intent_id TEXT UNIQUE,gross_cents INTEGER NOT NULL,provider_cents INTEGER NOT NULL,platform_cents INTEGER NOT NULL,partner_cents INTEGER NOT NULL DEFAULT 0,currency TEXT NOT NULL DEFAULT 'eur',partner_ref TEXT,status TEXT NOT NULL DEFAULT 'created',refunded_cents INTEGER NOT NULL DEFAULT 0,created_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP,updated_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP)`).run();await env.DB.prepare("CREATE INDEX IF NOT EXISTS idx_experience_providers_provider ON experience_providers(provider_id)").run();await env.DB.prepare("CREATE INDEX IF NOT EXISTS idx_booking_settlements_provider ON booking_settlements(provider_id)").run();await env.DB.prepare("CREATE INDEX IF NOT EXISTS idx_booking_settlements_experience ON booking_settlements(experience_id)").run()}
async function generatePartnerRef(env,name){const base=name.normalize("NFD").replace(/[\u0300-\u036f]/g,"").toUpperCase().replace(/[^A-Z0-9]+/g,"").slice(0,8)||"PARTNER";for(let i=1;i<1000;i++){const candidate=base.slice(0,12)+String(i).padStart(3,"0");const existing=await env.DB.prepare("SELECT id FROM partners WHERE partner_ref = ? LIMIT 1").bind(candidate).first();if(!existing)return candidate}throw new Error("Kein freier Partner-Code verfügbar.")}
function buildPartnerLink(partnerRef){return "https://getlocalis.pages.dev/?ref="+encodeURIComponent(partnerRef)}
function buildQrUrl(partnerRef){return "https://api.qrserver.com/v1/create-qr-code/?size=500x500&data="+encodeURIComponent(buildPartnerLink(partnerRef))}
async function getPartnerStats(env,partnerRef){if(!env.STRIPE_SECRET_KEY)throw new Error("Stripe secret not configured");const payments=await searchPartnerPayments(env,partnerRef);const successful=payments.filter(payment=>payment.status==="succeeded");let revenueCents=0;for(const payment of successful){const receivedCents=Number(payment.amount_received||payment.amount||0);const refundedCents=await getSuccessfulRefundAmount(env,payment.id);revenueCents+=Math.max(receivedCents-refundedCents,0)}const commissionCents=Math.round(revenueCents*0.03);let paidCents=0;if(env.DB){await ensurePayoutsTable(env);const payoutResult=await env.DB.prepare("SELECT COALESCE(SUM(amount_cents), 0) AS paid_cents FROM partner_payouts WHERE partner_ref = ? AND status = 'paid'").bind(partnerRef).first();paidCents=Number(payoutResult?.paid_cents||0)}const openCommissionCents=commissionCents-paidCents;return{partnerRef,bookings:successful.length,revenue:revenueCents/100,commission:commissionCents/100,openCommission:openCommissionCents/100,paidCommission:paidCents/100,currency:"eur"}}
async function searchPartnerPayments(env,partnerRef){const allPayments=[];let page="";for(let i=0;i<100;i++){const query="metadata['partner_ref']:"+"'"+partnerRef.replace(/'/g,"\\'")+"'";const stripeUrl="https://api.stripe.com/v1/payment_intents/search?query="+encodeURIComponent(query)+"&limit=100"+(page?"&page="+encodeURIComponent(page):"");const stripeResponse=await fetch(stripeUrl,{method:"GET",headers:{"Authorization":"Bearer "+env.STRIPE_SECRET_KEY}});const data=await stripeResponse.json();if(!stripeResponse.ok)throw new Error(data?.error?.message||"Stripe error");allPayments.push(...(data.data||[]));if(!data.next_page)break;page=data.next_page}return allPayments}
async function getSuccessfulRefundAmount(env,paymentIntentId){let refundedCents=0;let startingAfter="";for(let i=0;i<100;i++){let stripeUrl="https://api.stripe.com/v1/refunds?payment_intent="+encodeURIComponent(paymentIntentId)+"&limit=100";if(startingAfter)stripeUrl+="&starting_after="+encodeURIComponent(startingAfter);const stripeResponse=await fetch(stripeUrl,{method:"GET",headers:{"Authorization":"Bearer "+env.STRIPE_SECRET_KEY}});const data=await stripeResponse.json();if(!stripeResponse.ok)throw new Error(data?.error?.message||"Stripe refund lookup error");for(const refund of data.data||[])if(refund.status==="succeeded")refundedCents+=Number(refund.amount||0);if(!data.has_more||!(data.data||[]).length)break;startingAfter=data.data[data.data.length-1].id}return refundedCents}
function json(data,status,corsHeaders){return new Response(JSON.stringify(data),{status,headers:{"Content-Type":"application/json",...corsHeaders}})}
